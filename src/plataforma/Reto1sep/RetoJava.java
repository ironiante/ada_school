package plataforma.Reto1sep;

import java.util.Scanner;

public class RetoJava {
    public static void main(String[] args) {

    }

    //Crear un método que se llame inputMatrix y que solicite por consola valores de tipo int para llenar
    // una matriz bidimensionales de 2 x 3 y que retorne dicha matriz (Parametros Scanner y un int para numerar
    // la matriz).
    //_________________________________________________________________________________________________________
  /* int inputMatriz() {
        Scanner entry = new Scanner(System.in);
        System.out.println("posiciones del arreglo");
        int cant = entry.nextInt();
        // instanciamos el arreglo
        int[] arreglo = new int[cant];
        for (int i = 0; i < arreglo.length; i++) {
            System.out.println("ingrese un valor");
            int nuevo = entry.nextInt();
            arreglo[i] = nuevo;
            return arreglo[i];

        }


    }*/
}

